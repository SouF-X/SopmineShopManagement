window.SopmineRuntimeConfig = Object.freeze({
  // Leave blank when the API is served on the same domain as the frontend.
  // Example for split hosting: "https://api.example.com"
  apiOrigin: "",

  // Optional full API base URL. When set, this wins over apiOrigin.
  // Example: "https://api.example.com/api/v1"
  apiBaseUrl: "",
});
